ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        host = "127.0.0.1"
    }
    application {
        modules = [ com.Table.Server.ApplicationKt.module ]
    }
}

jwt {
    domain = "127.0.0.1/"
    audience = "L4GUser"
    realm = "L4GApp"
}